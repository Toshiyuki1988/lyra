# LYRA — 実装ハンドオフ

CONSTELLATIONの姉妹アプリ。DTMプラグイン/実楽器/ジャンル/舞台のマニュアル・文献を読解・分解・体系化し、Cubaseでの作曲作業を後押しする専用Webアプリ。本ドキュメントは、claude.aiでの構想フェーズ(会話+Design canvasモックアップ)からClaude Codeでの実装フェーズへ引き継ぐための仕様まとめ。

デザインモックアップ(Claude Design canvas、3画面): https://claude.ai/artifact/5ZNaLfpFBKFjzoBH94tCUN
アプリアイコン案(竪琴+こと座モチーフ、採用保留): https://claude.ai/artifact/6heq43WRxJM3ZjHbTKdk1e
旧概念ドキュメント(かなり古い、本ドキュメントが上位): `/mnt/user-data/uploads/LYRA_concept.md`

## 1. 位置づけと開発方針

- 実際の作曲作業は常にCubase Pro 15 + Max 9で行う。LYRAはそれを代替しない。「制作に煮詰まった時に立ち寄る」場所であり、日常的に往復する場所ではない
- CONSTELLATION内部の拡張タブではなく、**新規リポジトリ・新規Webアプリ**として作る。APIキー入力ダイアログ・Drive認証・interact.jsキャンバス初期化・ヘックスメニュー等の共通基盤は、CONSTELLATIONのコードから最初にLYRA用リポジトリへコピーしてから着手する(あとから移植ではない)
- GCPプロジェクト・OAuthクライアントは同一オリジン(toshiyuki1988.github.io)のためCONSTELLATIONと共有可能
- 著作権ゲート(必須。詳細は8節):構造的にエクスポート/共有機能を作らない。出典抜粋は要約で圧縮。出力は構造化データ+短い説明に留め、原文を丸ごと出さない

## 2. 用語グロッサリー(最終版)

複数回の再検討を経た確定形。旧称は参考のため併記。

| 現行 | 意味 | 旧称の変遷 |
|---|---|---|
| **LYRA** | アプリ名 | — |
| (無名) | 1つ目の画面。全ソウルを俯瞰する入口。「スタートメニュー」的な性質のため、あえて固有名詞をつけない | バックステージ→アーカイブ→ランチャー→無名化 |
| **ソウル** | プラグイン/実楽器/ジャンル/舞台など、対象ごとに文献解体で育つ専門AIの総称、およびそれを育てる部屋(画面)の名前。「Serum2のソウル」のように必ず対象名とセットで使う | ノートブック→スタジオ→(一時)精霊→ソウル |
| **アンサンブル** | 2つ目の画面(旧称の変遷先)。ソウルたちがAsterism接続から共同で発言する行為、およびその行為が起きる画面のナビ表記。画面名は「行為」で表す方針 | キャンバス→コンサートホール→会場キャンバス→(一時)舞台→アンサンブル |
| **舞台** | 概念語として存続。個別の会場(コンサートホール型・チャペル型・洞窟型など)を指す「舞台のソウル」という区分・カテゴリ表記に限定して使う(例:「舞台 · 55%」)。画面名としては使わない | (コンサートカード→バイオームカード→廃止、を経て残った区分語) |
| **課題** | ユーザーから、またはアプリから持ち込まれる「今取り組むべきこと」。アンサンブル画面上を漂うカードとして存在し、近くに置かれた他のカードとの偶然の接続がアンサンブルの引き金になる。デイリーの自動提示機能(旧称「今日の一手」)もこの一種 |  |

廃止された概念:「バイオームカード」(空間AIをアンサンブル上で漂わせ、色のにじみ領域で他カードに雰囲気を持ち込む仕組み)。複数バイオームの領域交差の効果が薄いと判断し廃止。代わりに個別の舞台自体をソウルとして育て、招集メカニズムに一本化した(4節参照)。

## 3. カード種

ノートブック(=ソウル)配下、またはアンサンブル画面上に浮かぶ:

1. **テキスト** — 解体済みの知識片。CONSTELLATIONの「テキストカード」に相当
2. **出典** — PDF/Web記事/YouTube動画。CONSTELLATIONのAlmagest相当。種類に`screenshot`(プラグインUI画像)を含む
3. **ソウル(専門AI)** — 各対象に専属。自分の出典・テキストカードのみを根拠にする(グラウンディング制約は5節)
4. **MIDI** — 提案されたMIDI。アプリ内簡易シンセで構造確認用に試聴可能(音色ではなくリズム・音階の確認が目的)
5. **オーディオ** — Cubaseで実際にバウンスしたWAVをドロップ。試聴シンセの書き出しにも使う
6. **課題** — 2節参照
7. **(出力専用)パラメータレシピ** — テキスト。7節参照

## 4. 画面構成

### 4.1 (無名の入口画面)
全ソウル(プラグイン/実楽器/ジャンル/舞台)を進行度リングつきの「星」として自由配置で一覧表示。俯瞰性を重視し、上から下へ流れるリスト形式は採用しない。ソウル同士を結ぶAsterism線は引かない(各ソウルは個室であり、越境はアンサンブル画面での招集によってのみ起きる、という設計と整合させるため。ただし視覚的に並べておくことで、たまたま近くにあるものを見て「これとこれ、繋げてみたら」という気づきは起きやすくする)。検索欄、今日の課題カードも浮遊配置。

### 4.2 ソウル画面(個々の対象の育成画面)
ハブ(ソウル本体)→モジュール(Osc A/Filter等の機能グループ)→パラメータ(葉)という階層構造。

- **複数タブ/複数画面を持つプラグイン**(Serum2のOsc/Filter/Matrix/FX/LFO等)は、モジュールごとにページタブを切り替える方式。各ページに、そのモジュールのUIスクリーンショットを貼り付け(従来のノートの使い方=気が向いたページから随時貼っていく。全ページ埋まっている必要はない)
- **パラメータの位置**:スクリーンショットの上に、パラメータノードをユーザーが実際のUI上の位置に合わせて自由にピン留めする(マニュアルとUIの照合作業を、読解時点で一度だけ解決しておく設計。CONSTELLATIONの「Mapping Storys」と同型の仕組み)
- **接続線は手動**:ハブ→モジュール→パラメータの接続(Asterism)は、自動生成ではなくユーザーが実際に手で結んだものだけを表示する(自動生成だとスクリーンショットの上で線が交差して見にくくなるため、2026-09-24に自動→手動へ変更)
- **進行度表示**:テキストラベル(draft/verified等)は使わない。全体の解体進行度は%バーで、個々のパラメータの状態はドットの塗り分け(点線=未確認、実線=確認済み)のみで表現する
- **非プラグイン系**(実楽器・ジャンル・舞台)は「UI」という概念を持たないため、この背景スクリーンショット方式は使えず、従来の抽象的な星図/ツリー背景のままになる見込み(要検討・未確定)

### 4.3 アンサンブル画面
自由配置キャンバス(CONSTELLATIONの星図と同型)。パラメータ・気づき・出典・オーディオ・MIDI・課題カードが、各ソウルから持ち出されて自由に散らばる。

- 接続(Asterism)はユーザーが手で結ぶ(偶然近くにあるカードを見て繋いでみる、という体験を重視)。**招集(どのソウルをアンサンブルに呼ぶか)だけが自動**:Asterismで繋いだカードの持ち主のソウルが自動的に招集される。手動でAIを選ぶ操作はない
- **舞台のソウルの二重役割**:個別の舞台(コンサートホール/チャペル/洞窟等)のソウルは、(1)自分がホームの舞台ではその舞台=アンサンブルの場そのものとして機能し、(2)別の舞台でのアンサンブルにはゲスト参加者として招集されることもできる。これにより旧バイオームカードの「場の雰囲気を持ち込む」機能は、通常の招集メカニズムに統合される形で温存されている
- 見た目:白基調+五線譜を思わせる薄い罫線パターン。ホームの舞台自身の控えめな暖色アンビエンス(舞台ごとに固有色)を背景全体にごく薄く敷く

## 5. ソウル(専門AI)の構成とグラウンディング

- **汎用ペルソナ(1つ)**:楽典・音楽理論特化。特定プラグインに紐付かないため、Geminiの一般知識をそのまま使ってよい
- **対象専属ソウル(無数)**:プラグイン/実楽器/ジャンル/舞台ごとに存在。**必ず自分の出典・テキストカードのみを根拠に発言する**(手持ちにないプラグインを提案させないための構造的制約)。「香りのシグネチャーシンセ」等プロジェクト固有の専門家アイデアもこの枠組みに吸収される
- **新しいソウルの追加条件**:星図の共通構造語彙(密度・明度・動き・空間・緊張、滲み・間・揺らぎ等)への変換表を最初に作ること。ソウルは単に知識を語るのではなく、必ずこの共通語彙に翻訳できなければならない
- **歴史文脈を語る場合**は出典必須ルールが同様に適用される(出典ページを示せない記述は生成させない。ハルシネーション対策、検索グラウンディングは無料枠で使えない前提)
- **提案の型**:常に「楽曲コンセプト・文脈 → 星図の構造語彙(共通言語) → 具体的な音色・操作」という3段階の連鎖を言語化して提示する。単発の相談より、複数ソウルのアンサンブルでこそ発見が起きやすい

## 6. 成長ルール

ソウルの成長は3〜4軸の掛け合わせ(実装の優先度は要相談):

1. **量**:解体の進行(確認済み/全体パラメータ数の%バー)
2. **質**:気づき(「気づきを書く」で書き戻されるテキスト)の厚み
3. **検証**:アンサンブルでの実戦フィードバック(効いた/効かなかった、が気づきとしてソウルへ書き戻される)
4. **構造の成長**:プラグインのアップデート等で新しいモジュール/パラメータが増える、新しい属性に気づく等

舞台型ソウルも、プラグイン等と全く同じ「文献解体→専属AI成長」パイプラインで育つ(例:洞窟の音響特性に関する論文を投入すると解体・吸収して育つ)。

**文献投入は随時追加可能**:初期セットアップ時に限らず、後からアップデート版マニュアルやレビュー記事などをいつでも追加していける。追加のたびに前段の解体パイプライン(下記)を通す。

## 7. 解体パイプラインと出力形式

**解体API呼び出し方針**:回数節約より正確さ優先。1回のAPI呼び出しに全部詰め込む単発処理ではなく、複数回に分けてよい(例:まず構造を把握する1回→モジュール/セクション単位で詳細抽出する複数回)。Gemini APIのPDFネイティブ入力+`responseSchema`構造化出力を使う想定。

**出力形式(Cubase/Serum2への持ち出し手段)**:
1. MIDIノート(旋律・リズム・コード進行提案)
2. MIDI CCオートメーション — 同じ.midにCCレーンとして埋め込み、Serum2のMIDI Learnでノブ割当て。連続値パラメータ(Cutoff、Resonance等)向け
3. WAVレンダリング(OfflineAudioContextでその場で書き出し、テクスチャ/ドローン提案の仮音源に)
4. テンポトラック(SMFテンポメタイベント)
5. マーカー(SMFマーカーメタイベント、星図の構造語彙で区切ったセクション)
6. **パラメータレシピ(テキスト)** — Warp Modeやフィルタータイプ等の離散選択式パラメータ、マクロ設定向け。MIDI/CCでカバーできない部分を、「パラメータ名:値」の一覧としてコピー可能なテキストで出力

キースイッチ/ドラムマップの音源固有ノート番号の取り決めはテキストカード化しておく(音符とキースイッチの混同防止)。

## 8. 著作権・データ量のガード

- 出典・テキストカードに共有/公開/エクスポート機能を実装しない(外部公開の経路自体を作らない)
- 出典カードへのPDF抜粋は長さ上限を課し、超過分はGeminiに自分の言葉で要約させる
- ソウルの提案は根拠テキストを丸ごと引用せず、構造化データ+短い説明に留める
- WAVスナップショットはMP3/Opus等の圧縮音声+波形概形(peaks JSON)で保持し、本当に聴き直したいものだけ個別に高音質フラグを立てる二段構え(無料Googleアカウント15GBの制約対策)
- YouTube埋め込みはiframeでURL文字列のみ保存(容量を圧迫しない)。Geminiによる動画解析とは明確に切り離す

## 9. 日次エンゲージメント機能(旧称「今日の一手」)

未検証カードから、放置期間や接続の少なさを基準に1つ選定 → 「なぜ今日これを試すのか」をジャンル/楽器文脈を絡めて一言添える → 確認済みに変わるたびキャンバス全体の見た目(進行度)が育つ、という3要素の統合。アプリ側が自動生成する「課題」の一種として位置づけ。

## 10. 未解決・要検討事項

1. 非プラグイン系ソウル(実楽器・ジャンル・舞台)の育成画面の背景表現(抽象的な星図/ツリーのままか、別案が要るか)
2. 同一パラメータについて複数資料(初版マニュアル・追補PDF・レビュー記事)で説明が食い違った場合の扱い(上書きか併記か)
3. 成長の見た目:進行度バー以外に「気づきの厚み」「現場検証済み」をどう視覚化するか
4. パラメータレシピの中身の粒度(「パラメータ名:値」のみか、音楽的意図も添えるか)
5. 日次課題の提示タイミング(開いた瞬間か、通知か)

## 11. 関連ドキュメント

- `/projects/.../topics/gear.md` — 音楽制作機材リスト(LYRA用の補完コピー)
- `/areas/hanagasumi-album.md` — 花霞アルバム(ジャンル深堀りの主要素材)
- `/areas/paint-synth.md` — ペイントシンセ構想(将来的な連動候補)
