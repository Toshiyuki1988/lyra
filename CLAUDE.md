# LYRA — 開発メモ

CONSTELLATION(美術鑑賞記録アプリ)の姉妹アプリ。DTMプラグイン/実楽器/ジャンル/舞台のマニュアル・文献を
読解・分解・体系化し、Cubase Pro 15 + Max 9での作曲作業を後押しする個人用Webアプリ。
**仕様の正本は `LYRA_handoff.md`**(claude.aiでの構想フェーズからの引き継ぎ)。このファイルは実装上の
約束事と、実装が進むにつれて判明した経緯を記録する。

- リポジトリ: https://github.com/Toshiyuki1988/lyra (Public、GitHub Pagesのため)
- 公開URL: https://toshiyuki1988.github.io/lyra/
- CONSTELLATION本体: `C:\Users\Toshiyuki\Desktop\constellation`(共通基盤のコピー元。LYRAの作業中にそちらを編集しない)

---

## 絶対条件

- **完全無料運用**。Gemini APIキーのプロジェクトには請求先アカウントを絶対に紐付けない
  (無料枠超過時は課金でなく429エラーになるようにする)
- **GCPプロジェクト・OAuthクライアントはCONSTELLATIONと分ける**(2026-09-24決定)。
  理由は README.md「CONSTELLATIONとの分離」参照(Gemini無料枠の取り合い防止/`drive.file`の
  アクセス範囲をOAuthクライアント単位で分離)。**LYRAのコードからCONSTELLATIONのDriveファイルや
  localStorage(`constellation.`で始まるキー)に触れる設計は作らない**
- **Driveのファイルをアプリ側から削除・上書きしない**。`js/drive.js`に削除関数は意図的に持たせていない
  (CONSTELLATIONで、完全削除と分かりにくい確認ダイアログが重なり、実機で二度と戻せない写真を失った
  事故があったため)。「使わなくする」時はカード側の参照を外すだけにする
- **著作権ゲート**(ハンドオフ8節): 出典・テキストカードに共有/公開/エクスポート機能を作らない。
  出典の抜粋には長さ上限、ソウルの提案は構造化データ+短い説明に留め、原文を丸ごと出さない
- **Geminiモデルは `gemini-3.5-flash-lite`**(無料枠1日250回、2026-09-24ユーザー決定)。
  Liteは込み入った構造化抽出で精度が落ちることがCONSTELLATIONで実機確認されているため、
  解体パイプラインは (1) 1回に詰め込まず「構造把握→モジュール単位の詳細抽出」に分ける、
  (2) `responseSchema`(`askGeminiJson()`)で出力形式を固定する、(3) 抽出結果は「未確認(点線)」
  として入れ、人が「確認済み(実線)」にする、の3つで補う。フル版モデルへ戻すのは無料枠が小さすぎる
  ため選択肢に入れない
- **検索グラウンディング(`google_search`ツール)は使わない**。請求先非紐付けの無料キーでは即429になる
  ことがCONSTELLATIONで実機確認済み。`askGemini()`は`tools`引数自体を受け付けない作りにしてある

## 開発方針(CONSTELLATIONから引き継ぐもの)

- **根本原因への到達を早期に検証する**: 不具合調査では、仮説の延長で対策(パラメータ緩和・当たり判定の
  拡大等)を重ねる前に、無条件の診断ログ(`?debug`付きURLで表示される`debugLog()`)や実機再現で
  「本当にそこで起きているか」を事実で確認してから修正する
- **A/B二択に`window.confirm()`を使わない**。`showChoiceDialog()`(`js/app.js`)でラベル付きの対等な
  ボタンにし、元に戻せない側は`danger: true`で赤くする
- **背景タップで閉じるオーバーレイは`attachBackgroundTapToClose()`を使う**(単純なclick判定は
  スマホで誤って閉じやすい)
- **JS/CSSを編集したら、`index.html`の対応タグの`?v=YYYYMMDDx`を必ず更新する**(キャッシュで古い
  JSが読まれ続け「実装したはずの機能が出ない」事故がCONSTELLATIONで実際にあった)
- **全JSファイルはグローバルスコープを共有する**。トップレベルの`const`/`let`/関数名が衝突すると
  そのスクリプトごと読み込みエラーで死ぬ。大きな機能を別ファイルに切り出す時は
  `(function () { ... })();`で包む
- **保存系の最終防波堤**: `js/app.js`の`dataLoaded`がfalseの間(Driveから読み込む前)は保存処理
  自体を実行しない。読み込み前の空stateでDriveを上書きする事故を防ぐため。起動シーケンスを分割・遅延
  させる変更をする時も、この防波堤を外さない
- **高頻度イベント(pointermove)から「全部を引き直す」関数を呼ばない**。Asterism線はドラッグ中は
  `updateAsterismLinesForCard()`の軽量パス、フル再構築は指を離した時だけ
- **キャンバス上の写真は`background-image`ではなく`<img>`で表示する**(`transform: scale()`の
  ズーム下で`background-image`の実解像度が追従しないモバイルの既知の制約)。`<img>`には
  `-webkit-user-drag: none`を最初から付ける(ネイティブドラッグがカードの複製に見える不具合の予防)
- **重要な操作の入口をジェスチャーだけに頼らない**。確実に押せるボタンを必ず併設する
- モックアップを作る場合は単体HTMLで検証してから本実装する(CONSTELLATIONと同じ運用)

## 共通基盤(2026-09-24、CONSTELLATIONからコピー)

| ファイル | 由来 | 変更点 |
|---|---|---|
| `js/auth.js` | そのままコピー | なし |
| `js/drive.js` | コピー | ヘッダーコメント、`moveFile()`(移行専用)を削除、`loadData()`の初期値をnullに |
| `js/canvas.js` | コピー | Flight Engineer/Star Pencil/座談会など、CONSTELLATIONのモジュール固有のガードを除去 |
| `js/sound.js` | 先頭の共通部分のみ | 編集ガイド・ASTR・カード移動の4音だけ残した |
| `js/gemini.js` | `askGemini()`のみ流用し書き換え | `files`(PDF/画像のインライン入力)・`responseSchema`対応、`askGeminiJson()`追加、タイムアウト90秒、`tools`撤去 |
| `js/config.js` | 新規 | `lyra.`始まりのlocalStorageキー、スコープは`drive.file`のみ、フォルダ名`LYRA` |
| `js/app.js` | 新規(必要な関数だけ移植) | 設定・サインイン・保存・テキストカード・ASTR手動接続・`showChoiceDialog()` |

- **LYRAのAsterismは手動接続のみ**(自動の「見た順」線は持たない、ハンドオフ4.2/4.3節)。
  唯一の例外は**MIDIの改善版**: MIDIカードの「作り直す」にコメントすると、改善版(_v2.mid…)が右隣に置かれ、
  元のカードと自動で線がつながる(ユーザー要望、2026-09-25。改善の系譜を辿れるようにするため。
  `connection.auto: true`)。**同じ例外で、できたMIDIカードは生んだカードから必ず線でつなぐ**(「鳴らす」=課題カード、
  「MIDIにする」=発言カード。ユーザー要望「どこに生成されたかわからない時がある」)。共通の`placeMidiCard()`(js/midi.js)が
  線・生成音(`playMidiCreatedSound()`「ポロロン」、js/sound.js)・`revealEnsembleCard()`(画面の外なら真ん中まで動かし、
  光らせる)をまとめて行う。気づきへの感想カードも自動で線を結ぶ(感想とまとめの節)
- 編集ガイドのヘックスは Edit=左、Delete=右、**ASTR=カードの下の外側**、気づき⇔課題の入れ替え(「→課題」「→気づき」)=左下の外側
  (2026-09-25追加。文・位置・線はそのまま型だけ変える。アプリからの課題は理由・対象のパラメータを持つので対象外)。ASTRは以前は中央だったが、
  テキスト編集中に文字を隠して邪魔という指摘で移した
- キャンバス背景は五線譜風の薄い罫線(ハンドオフ4.3節)。ドット格子ではない

## 2026-09-25 ユーザー決定(ハンドオフ10節の未決事項ほか)

- **アンサンブルの発言は「アンサンブルを聴く」ボタンを押した時だけ**。Asterismで繋いだカードの持ち主の
  ソウルが招集される(アバターが並ぶ)ところまでは自動だが、Gemini呼び出しは明示操作のみ(無料枠保護)
- **非プラグイン系ソウル**(舞台・作曲家・美学など)は将来それぞれ違う見た目になる見込みだが、当面は
  スクショ背景なしのカード自由配置型(プラグインと同じソウル画面で、スクショを貼らないだけ)
- **同一パラメータの説明が資料ごとに食い違ったら、上書きせず出典ごとに表示**(`param.readings[]`)
- **説明の2欄(effect / intent)の意味はソウルの区分で変える**(`js/app.js`の`READING_FIELDS`/`readingFields()`)。
  以前は全区分を「音響的効果・音楽的意図」で抽出していて、美学の視覚的な特徴まで音の言葉に寄せて書かれていた
  (ユーザー指摘「美学そのものの視覚的特性も残してほしい」)。**美学は「美学的特徴」(視覚・色・質感・モチーフ・
  雰囲気を音に言い換えずに残す。原文は写させず自分の言葉で要約させる。プロンプトに「そのまま」と書かない)と「音への翻案」(音楽にするなら、推測なら「(推測)」)**。ジャンル・作曲家・
  舞台・実楽器もそれぞれの見出し。解体のプロンプト・ソウル画面/アンサンブルのパネル・アンサンブルに渡す知識・
  日次課題で同じ定義を使う。既に解体した資料は「もう一度解体」で新しい形に書き直される。
  「実楽器の奏法対応」欄はプラグインだけ(他の区分は既に値がある時だけ表示)
- **成長の見た目は当面、進行度リング+%のみ**
- **パラメータレシピは「パラメータ名: 値」に音楽的意図を一言添える**
- **日次課題はアンサンブル画面に毎日「課題 · アプリから」カードとして追加**される(ユーザーが自分で
  置く課題カードと同じ扱い)。設定で停止できる
- **アンサンブル画面は舞台のソウルごと**。デフォルトは「アンサンブル in コンサートホール」
  (コンサートホールは初期状態から存在する舞台のソウル)。舞台のソウルが増えるたびに
  「アンサンブル in 〇〇」が増える。切り替えは**ヘッダーの舞台タブ**(1クリック)。当初はプルダウンにしたが、
  実際に使って「アンサンブル→コンサートホールの2段階クリックが手間」というユーザー判断でタブに変えた(同日)
- ソウルの区分: プラグイン / 実楽器 / ジャンル / 舞台 / 作曲家 / 美学
- 開発は段階ごとにpushし、実機確認を挟みながら最後まで進める
  (1.入口画面 2.ソウル画面 3.解体パイプライン 4.アンサンブル 5.出力(MIDI/WAV/レシピ) 6.日次課題)

## 画面とファイルの構成(2026-09-25、段階1〜6を実装)

| ファイル | 役割 |
|---|---|
| `js/app.js` | 中核。データモデル(`state.souls` / `state.ensembles` / `prefs` / `daily`、version 2)と旧形式からの移行、ハッシュでの画面切り替え(`#/`・`#/soul/<id>/<moduleId>`・`#/ensemble/<stageId>`)、カード描画とASTRの共通処理、右パネル、`showChoiceDialog()`/`showFormDialog()` |
| `js/screens/home.js` | 入口画面(ソウルの星・進行度リング・検索・新しいソウル・今日の課題) |
| `js/screens/soul.js` | ソウル画面(左のモジュールタブ、スクショへのピン留め、右のパラメータ詳細) |
| `js/screens/ensemble.js` | アンサンブル画面(カード各種、自動招集、「アンサンブルを聴く」、効いた/効かなかったの書き戻し) |
| `js/decompose.js` | 資料パネルと解体パイプライン、共通語彙の変換表、スクショからのパラメータ読み取り |
| `js/midi.js` | MIDIカード(発言→MIDI、SMF書き出し、簡易シンセ試聴、OfflineAudioContextでのWAV書き出し) |
| `js/audio.js` | オーディオカード(Opus/Oggへの圧縮+peaks、Drive保存、Web Audioで再生) |
| `js/daily.js` | 日次課題(1日1回、未確認パラメータから選んでコンサートホールのアンサンブルへ) |
| `js/marquee.js` | 矩形選択と一括移動(Shift+ドラッグ。CONSTELLATIONのFlight Engineerの簡易版。全画面共通) |
| `js/scales.js` | 音階スケールの一覧(内蔵72種+Geminiにたずねて足したもの)とリスケーリング(MIDIの編集画面から) |

- **画面の仕組み**: `applyRoute()`が画面モジュール(`LYRA.screens.*`)の`enter(route)`を呼ぶ。画面は
  `scope = { cards, connections }`に「今のキャンバスに載せるカード配列・線の配列」を渡すだけで、
  `js/canvas.js`由来の`getCardById()`・`createAstrConnection()`などはscopeに対して働く。
  画面側は`buildCard / cardHexes / onHexAction / onCardTap / onCardMoved / onConnectionsChanged /
  afterRender / leave`だけを実装する。`connections: null`の画面(入口画面)ではASTRを出さない
- **scopeの配列は元データそのものを渡す**(入口=`state.souls`、アンサンブル=`ensemble.cards`、
  ソウル画面の線=`soul.connections`)。削除は`removeCardFromScope()`がspliceで行うので、
  **scopeに渡した配列を`filter()`で作り直さない**(参照が切れて保存されなくなる)。ソウル画面のcardsだけは
  `[module, ...そのモジュールのparams]`の派生配列なので、パラメータ削除時は`soul.params`側も別途外す
- 画面ファイル・機能ファイルはすべて`(function () { ... })();`で包み、外へ出す関数は`window.*`か
  `LYRA.screens.*`に明示的に置く(全JSがグローバルスコープを共有するため)
- **解体パイプライン**: 構造把握(モジュール名+パラメータ名だけ)→モジュールごと(20件ずつ)の詳細抽出
  →共通語彙の変換表、の順。呼び出しの間は4.5秒空ける(無料枠のRPM対策)。途中で止まったら
  `source.pending`に進み具合が残り「続きから解体」で再開できる。PDFはGemini Files APIへ1回上げて
  URIを使い回し、Files APIがブラウザから使えなかった時だけ18MBまでのインライン送信に切り替える
  (**Files APIのブラウザからの動作は実機未確認**。失敗時は`?debug`のログに理由が出る)
- **PDFはページ単位で切り分けて渡す(2026-09-25、実機計測を受けて変更)**: Serum2の日本語マニュアル
  (36.6MB)は、全体を渡すと1回で入力約19.6万トークン・約88秒かかった(構造把握の実測)。モジュール23個
  ぶん毎回全体を渡すと30分以上かかるため、構造把握だけ全体(Files API、アップロード4.4秒で成功)を渡し、
  各モジュールの「PDF上の通しページ番号」(pdfStart/pdfEnd)も出させる。詳細抽出はそのページだけを
  pdf-lib(jsdelivr、必要な時だけ読み込む)で切り出した小さなPDFを渡す。ページ番号が欠けたモジュールは、
  他のモジュールの「印刷されたページ番号との差」の中央値で補う。切り出したページで目当てのパラメータが
  3割未満しか見つからなければ、前後8ページ広げて1回だけ読み直す
- **Geminiの一時的な失敗は自動で待って再試行**: 503(「high demand」、実機で発生)・500は20/40/60秒待って
  3回まで、429(1分あたり)は65秒待って1回。1日あたりの429はそのまま止める(続きから再開できる)。
  `askGemini()`の例外には`status`(と429なら`perDay`)が付く。`?debug`のログに各呼び出しの秒数・
  入力/出力トークン数・終了理由が出る
- **Geminiの出力の暴走対策**(2026-09-25、Aesthetics Wikiの記事の解体で「Expected ',' or '}' ... position 8306
  (line 13 column 8105)」で停止): 1行が8000字を超えていたことから、Liteモデルが同じ語句を繰り返し続けて出力上限で
  切れたとみている(終了理由は未確認)。`askGemini()`はJSONモードで終了理由がMAX_TOKENSならその旨のエラーにし、
  `askGeminiJson()`は出力が切れた・JSONが壊れていた時に1回だけ自動でやり直す。構造把握は1モジュール40項目まで、
  固有名詞の長い列挙は1項目にまとめるよう**プロンプトで**指示している。
  **responseSchemaに`maxItems`を付けると、このモデルでは400(INVALID_ARGUMENT)で拒否された**(実機で確認)。
  スキーマに型・properties・required以外の制約を足す時は、必ず実機で1回通してから使う
- 著作権ゲート: Web記事の本文は解体にだけ使い保存しない。抽出した説明は160字・140字で切り詰める。
  出典・テキストに書き出し機能は無い。書き出せるのはアプリが生成したMIDI(.mid)・WAV・パラメータレシピだけ
- **モジュールは「UI上の場所」(`module.location`、例: "FXタブ")を持つ**(2026-09-25追加)。Serum2の解体で
  「UTILITYがどこにあるか分かりにくい(FXタブの中)」という指摘を受けた。構造把握で一緒に出させ、左のタブは
  場所ごとに小見出しでまとめ、表示は`modulePath()`で「FXタブ › UTILITY」にそろえる(パンくず・詳細・レシピ・
  アンサンブルに渡す知識)。それ以前に解体したソウルは、資料パネルの「資料から整理する」でPDFを1回読ませて付ける。
  Files APIに上げたPDFのURIは`source.geminiFile`に残し、期限(48時間)内は使い回す
- **パラメータレシピ**(2026-09-25拡張): 発言に付くレシピは「音色づくりに関わる主要な設定を5〜12個」。
  レシピパネルの「初期状態から作る詳しいレシピにする」で、レシピ専用にGeminiを1回呼び、Initから画面の上から順に
  15〜30項目の手順を作る(`card.recipeFull`)。表示は手順の順番を崩さないよう、同じモジュールが続く間だけまとめる。
  「小窓で出す」はChrome/EdgeのDocument Picture-in-Picture(他のアプリより常に手前の小窓)でCubaseの上に浮かべる。
  **クリックなどの実際のユーザー操作からしか開けない**(自動テストで合成したクリックでは開かなかった)。対応して
  いないブラウザでは普通の別ウィンドウ。各行のチェックは`card.recipeDone`に保存
- **レシピの限界と保留(2026-09-25)**: 詳しいレシピで「Serum2のMultisampleを使う」と出ても、どのサンプル・
  ファクトリープリセットかまでは出ない。渡している知識が解体したパラメータ(マニュアル由来)だけで、同梱素材の一覧や
  プリセットの作り込み(複雑なモジュレーションの配線など)はマニュアルに無いため。名前を出させるとLiteモデルは
  実在しない名前をでっち上げやすい。**ファクトリープリセット級の複雑な音作りは、ユーザー判断で保留**。再開する時の
  候補: (1) Max 9の`vst~`でSerum2を読み込み、プリセットの全パラメータ値を読み出して解体する/レシピを流し込む
  (モジュレーションの配線やサンプル選択がホストから見えるパラメータとして出ているかは未確認)、(2) `.SerumPreset`
  ファイルの解析(形式は非公開)、(3) 手持ちの素材名の一覧をソウルに登録してレシピで選ばせる。画面を直接操作させる
  方法は無料運用の外なので対象外
- **コード+旋律(スケッチ、2026-09-25)**: 「生成したMIDIが面白くない。美学からつないだだけで、その美学を一聴で
  表すコード+メロディが出る構造に」という要望で追加。Liteに音番号を1音ずつ並べさせると和声も声部も平凡になるため、
  Geminiには**記号の設計図**(キー・旋法・コードネーム・伴奏の型・和音の積み方・ベースの型・ハネ・音名の旋律)と
  **signature**(ソウルのどの特徴を、どんな音楽の仕掛けで表すか3〜4個)だけを出させ、ノートへの展開(コードネームの
  解析・声部進行の近さで選ぶ転回形・伴奏のリズム・ベース・ハネ)は`js/midi.js`の`renderSketch()`が決まった手順で
  行う。設計図は`card.midi.sketch`に残し、作り直しも設計図でやり取りする。ノートは`part`(melody/chords/bass)
  付きで、.midでは別トラック(ch1〜3)。入口は、課題カードの「鳴らす」ヘックス(美学・ジャンル・作曲家のソウルが
  そのまとまりに招集されている時。線をつないだだけではGeminiを呼ばない=無料枠保護の決まりどおり)と、
  発言の「MIDIにする」の既定の選択肢。プラグインのソウルの知識は渡さない。**実機のGeminiではまだ未確認**
  (出力の質、コードネームの書き方の揺れ)
- **MIDIのコンセプト・解説と盗作対策(2026-09-25)**: MIDIカードに`concept`(60字)と`commentary`(解説200字)を
  出させて、カード・パネル・小窓に表示する。コード進行・伴奏の型・旋法はジャンル共通の語法だが、**旋律はGeminiが
  書いた音をほぼそのまま使う**ため、既存曲の旋律・リフの引用や模倣、特定の曲・アーティストへの寄せを
  プロンプトで禁止している(`ORIGINALITY_RULE`)。既存曲と照合する手段は無い
- **主旋律は必ず反芻させる/コード進行は定番可/「鳴らす」にアーティスト名を渡さない(2026-09-25、ユーザー判断)**:
  主旋律を作る経路(鳴らす・発言の「MIDIにする」の旋律だけ・それらの作り直し)では、1回目の後に`ruminateMelody()`で
  もう1回Geminiを呼び、既存の有名な旋律・フックとの類似を音程の並びとリズムで点検させ、似ている所は書き換えさせる
  (コード進行は変えさせない)。**反芻に失敗したらカードを作らない**。点検の記録は`sketch.rumination`/`midi.rumination`
  でパネルに出す。旋律だけのMIDIは`midi.kind`で見分ける(種類の無い古いカードの作り直しは対象外)。
  **コード進行はジャンルに共有された語法なので、よく知られた定番進行も使ってよい**(「全体的にいい塩梅に」)。
  「鳴らす」の時だけ、アーティスト名・曲名などの項目(`isReferenceParam()`、js/app.js。項目名・モジュール名と
  説明の言い回しで判定)を知識からもつないだカードからも外す。知識そのもの(ソウル画面・発言)には残す
- **作曲家の技法の引用と、生成MIDIの自由度(2026-09-25、ユーザー要望「特定の旋律は用いずに、クラシックの作曲家の用いた
  技法(ポリコードなど)を要望を聞いてから引用してMIDIに反映」「途中で拍子を変えたり、2つのコードを重ねたり、かなり自由に」)**:
  - 「鳴らす」「MIDIにする」「作り直す」のダイアログに**「取り入れたい作曲家・技法(任意)」**を足した。書かれていれば
    `techniqueRule()`で、その作曲家が実際に用いた技法を2〜4個選ばせて必ず使わせ、`techniques`(composer・work=技法の出典の
    代表作・technique・use)に書かせる。パネルに「引用した作曲技法」、カードに「技法: …」を出す。**引用してよいのは技法だけ**で、
    旋律・動機・特徴的なリズム音型・和声進行の並びはそのまま使わせない(`ORIGINALITY_RULE`も「作曲家の技法は技法として取り入れて
    よい」に改めた)。反芻では技法と大胆さを「無難に戻さない」よう指示する。作り直しはコメントが空でも、技法の要望かASTRのつなぎが
    あれば実行できる
  - **拍子の変化**: `midi.meters`/`sketch.meters` = `[{bar, num, den}]`(barは1始まり)。位置は従来どおり**曲頭からの通しの
    4分音符の拍**で、`beatsPerBar`は最初の小節の長さ(7/8なら3.5)として残す。小節の位置は必ず`barList()`/`barAt()`/
    `meterStarts()`で計算する(`beat / beatsPerBar`で小節を出さない)。伴奏の型・ベース・ピアノロール・編集画面の小節線と
    Shiftでの小節単位の囲み・範囲の書き出し(範囲の頭の拍子から付け直す)・.midの拍子メタイベント(変わる所ごと)が対応
  - **ポリコード**: コードネームを「C|F#」のように「|」で区切る(最大3つ。左が下)。`parseLayers()`。上のコードは密集で、
    下のコードの平均より5度〜1オクターブ半上に積む。「/」は従来どおり分数コード
  - **対旋律/オスティナート**: 設計図の`counter`(旋律と同じ形式)→ パート`counter`(.midでは別トラック、ピアノロールは青緑)。
    反芻の対象は主旋律だけ。編集画面で直した対旋律も設計図に書き戻す
  - プロンプトの`FREEDOM_RULES`で「無難にまとめず、拍子の変化・ポリコード・クラスター・旋法の混合・非機能的な進行・
    オスティナートなども自由に」と促している。**実機のGeminiでの出方(Liteが通しの拍を正しく数えられるか)は未確認**
- **MIDI生成のゲージ(2026-09-25、ユーザー要望)**: 「鳴らす」「MIDIにする」「作り直す」のダイアログに0〜100のスライダーを4本
  (`GAUGES`、js/midi.js。ダイアログの`type: 'range'`はjs/app.jsの`formFieldHtml()`に足した): **音の粒度**(長い音・疎〜細かい粒・密)、
  **メロディの跳躍度**(順次進行〜大きく跳ぶ)、**ダブ的なつんのめり度**(拍どおり〜つんのめる)、**感情のあるなし**(無機質〜感情豊か)。
  プロンプト(`gaugeRule()`、反芻にも渡して「書き換えても守る」)に加え、Liteが数値を守りきれない分をアプリ側で決まった手順で補う:
  つんのめり30以上で小節頭のコード・ベースを16分早く食う(65未満は1小節おき)、50以上で刻みの後に付点8分のこだま(80以上は2回。
  伸ばす伴奏と、次の刻みまで間が無い時は付けない)=`applyDub()`、コード+旋律のみ。感情は強弱の幅を0で平ら・100で倍に
  (`applyEmotion()`、全種類)。粒度・跳躍はプロンプトだけ。値は`sketch.gauges`/`midi.gauges`に残し、作り直しの初期値・パネル・
  アンサンブルへの説明に使う。同じパート・同じ高さの重なりは`trimOverlaps()`で前の音を切る
- **時間の設計図(序破急・起承転結)と、つないだMIDIのパートの差し替え(2026-09-25、ユーザー指摘「何かを計算して並べた感が強い。
  序破急や起承転結がない」+要望「アステリズムで外部からつなげたものを置き換える」「生成前にユーザーに質問を返してよい」
  「最小のAPI使用数で」)**:
  - 原因の見立て: 伴奏の型・積み方・ベースが曲全体で1つ/旋律に目的地(頂点・終わり方)が無い/「転」が無い/全体の起伏を
    考える段階が無い。光景だけ(静止画)では足りず、時間とともに変わる物語→起伏への置き換えが要る
  - **生成前の質問(APIなし)**: ダイアログに「型」(おまかせ/序破急/起承転結/AABA/ABA/一続きの物語)、「イメージ元の光景・物語」
    (つないだ気づき・課題カードの文が初期値。作り直しでは前回の物語)、ASTRでつないだMIDIごとに「そのまま使うパート」
    (使わない/旋律/コード/ベース/旋律とコード)。`narrativeFields()`/`readNarrative()`
  - **設計図 `sketch.arc`**(form・story・climaxBar・turn・sections[name, startBar, bars, scene, tension 0〜10, register,
    comping・voicing・bass, role, ending])を、音と**同じ1回の出力の先頭**に書かせる。キー名を`arc`にしてあるのは、構造化出力が
    アルファベット順に出る場合でも最初に来るようにするため(未検証の工夫。どちらでも害は無い)。`renderSketch()`がセクション
    ごとに伴奏の型(`none`で抜く)・積み方・ベース・和音の高さ(low/mid/high)を切り替え、緊張度で強弱を動かす(感情のゲージが
    大きいほど強く、0なら動かさない)。セクションはコードの頭の拍で決まる。反芻で「物語として聞こえるか」(頂点・動機の回帰・転・
    終わり方・息継ぎ)も点検させる。パネルに「時間の設計図」(緊張度のバー付き)、カードに「起承転結 · 頂点n小節」
  - **差し替え**: 改善の系譜(revisionOf で両方向)の外からつないだMIDI(`LyraMidiLinks().midis`、「鳴らす」ではまとまりの
    MIDI)のパートを固定としてGeminiに渡し(旋律固定ならmelodyは空に)、生成後に`applyFixedParts()`で差し替える。テンポ・拍子・
    ハネはつないだMIDIにそろえる(`alignToSource()`)。`midi.fixedFrom`に記録し、作り直しでは実際の音を渡す。
    パートの無いMIDIは全部の音をそのパートとして使う。差し替えはコード+旋律のカードのみ
  - **API回数**: 旋律を差し替える時は反芻を省いて1回、それ以外は2回(生成+反芻)のまま。旋律以外のMIDIは1回(設計図は
    プロンプトの指示だけ)
- **編集画面のテンポと試聴の音量(2026-09-25、ユーザー要望「MIDIエディターで再生速度、音量を調整できるように」)**:
  「テンポ」(BPM、20〜300)は下書きのテンポで、試聴の速さが変わり、試聴中なら頭から鳴らし直す。**保存すると`midi.tempo`
  (途中のテンポ変化は同じ比率で、設計図の tempo も)に書き戻すので、.midのテンポも変わる**(ノートは変えないので「手で編集済み」
  にはしない)。「試聴の音量」は全カード共通の`state.prefs.previewVolume`(0〜100、既定80=従来の音量)で、試聴の音は必ず
  `previewMaster()`のゲインを通るため再生中でもすぐ効く。WAVの書き出し(OfflineAudioContext)には効かない。
  テンポの数字を打っている間は、編集画面のキー操作(Backspace=音の削除など)を止める
- **スケールでリスケール(2026-09-25、ユーザー要望「MIDI編集画面で、Geminiが知る全ての音階スケールをプルダウンで選んで、
  MIDI全体をリスケーリング。Cubaseでもこのフローをよく行う」)**: 編集画面の「スケール」の行。一覧は`js/scales.js`に**内蔵**
  (72種: 教会旋法、和声的/旋律的短音階の旋法、ハンガリアン・ナポリタン・ペルシャ等、インドのターート、ビバップ、全音・
  ディミニッシュ・メシアン、ペンタトニック・ブルース、日本の音階など。毎回Geminiに聞くと無料枠を使うため)。一覧に無いものは
  「Geminiにたずねる」で1回だけ聞き、12平均律に丸めた半音の並びを`state.prefs.customScales`(Drive)に残す。方法は
  「近い音にそろえる」(真ん中なら下へ)と「度数を保って移す」(元のスケールの度数+半音の変化を保つ。音の数が違うスケールへは
  度数を比で対応させてからそろえる。ルートは±6半音の近い方へ)。元のスケールは設計図のキー・スケール名から、読めなければ音から
  メジャー/マイナーで推定。対象パートを選べる。ピアノロールにスケール内の行を薄く塗る。リスケールは下書きへの操作で
  「元に戻す」が効き、保存すると設計図の key・scale と`midi.rescaledTo`も書き換える。「▶ 音階を聴く」(と既定オンの「選んだら鳴らす」)で、
  選んだ音階をルートから1オクターブ上って下りるプレビューをカードの音色で鳴らす
- **BEAT生成(2026-09-25、ユーザー要望「MIDI生成と別でBEAT生成も。美学などからはそのままジャンルを象徴するビートをすぐ生成」
  「ビート、リズムには著作権がないはずなので、参照楽曲からも大いに影響を受けていいはず」)**: js/midi.js の BEAT の節。
  - 入口: 美学・ジャンル・作曲家のソウルカードの上の「ビート」ヘックス(そのソウル1体+つないだカードから、すぐ)、課題カードの
    「ビート」(聴く・鳴らす・ビートの3つ並び。まとまりのソウル+カード、ソウルが無くても課題の文だけで作れる)、発言の
    「MIDIにする」の「ビート」。できたカードは生んだカードから線でつながる(`placeMidiCard()`)
  - Geminiには**リズム譜の文字列の設計図**だけを出させる(genre・tempo・steps=1拍の分割・swing・meters・sections[{name, bars,
    pattern:[{inst, steps:"X...x...o..."}], fill}]・references・signature)。X=アクセント x=普通 o=ゴースト .=休み。
    `renderBeat()`がGMドラム(`DRUM_MAP`、27種)のノート(part 'drums')に展開し、ハネ(steps の裏)と感情のゲージのヒューマナイズを
    付ける。1小節より短い行は、1拍の倍数の長さなら繰り返し、それ以外は残りを休みにする(「X」だけのクラッシュが毎ステップ
    鳴った実例から)。fill は区間の最後の小節で、書いた楽器の行だけ差し替える。区間は markers になる
  - **参照曲・アーティストのビートの型・ノリは大いに取り入れてよい**とした(ユーザー判断。ドラムパターンはジャンルに共有された語法)。
    ただし1曲のドラムパートを頭から終わりまで写し取ることはさせない。旋律の縛り(ORIGINALITY_RULE)と反芻は無く、**Gemini 1回**。
    ソウルの知識も参照を除かず渡す。ゲージは粒度・つんのめり・感情の3本(`BEAT_GAUGES`)
  - .midではドラムを**10ch**(GMの約束)に置く。試聴はドラムを合成音で鳴らし分ける(`drumHit()`。Webの音色は読み込まない)。
    リスケールはドラムに効かない。「作り直す」はビートの設計図でやり取りする(参照・ゲージも聞き直す)
- **小窓モード(`js/mini.js`、2026-09-25)**: ヘッダーの「小窓」でDocument PiPを開き、全アンサンブルのMIDIカードを
  新しい順に並べる(試聴・解説・Cubaseへのドラッグ)。アプリ全体を移さないのは、キャンバスが本体のウィンドウ前提で
  動いていて壊れるため。チップは「1トラックで」(全パートを1トラック・ch1に。既定のおすすめ)/「パート別3トラック」/
  パート1つずつ(`buildSmf(card, mode)`)。「全トラック」だけだった頃、Cubaseのインストゥルメントトラックに入れると
  3トラックに分かれてしまうと指摘され、1トラックにまとめる形を足した。MIDIのパネルにも同じチップがある。
  発言のレシピの小窓と同時には開けない(PiPは1ページに1枚)
- **MIDIの範囲を選んで書き出す(2026-09-25、ユーザー要望「いい感じの一部分だけを囲ってインポートしたい」)**: MIDIのパネルの
  「範囲を選ぶ」で編集画面(`openMidiEditor()`の「書き出す範囲を囲む」)を開き、時間(拍単位、Shiftで小節単位)×音域の四角を囲む。
  `card.selection = {start, end, low, high}`(拍・音番号、endは含まない)に残り、⇩のチップ・ドラッグ(パネル・小窓)は
  その範囲だけを書き出す(`exportCard()`/`sliceMidi()`)。範囲の頭を0拍目にずらし、頭・終わりをまたぐ音は範囲の中だけを
  切り出す(0.125拍未満のかけらは捨てる)。テンポは範囲の頭の値から。ファイル名に`_bars3-4`を添える。試聴・WAV・
  「.midを書き出す」ボタンは全体のまま
- **MIDIの編集画面とASTRでのブラッシュアップ(2026-09-25、ユーザー指摘「MIDI編集画面が分からなかった」)**: MIDIカードの
  編集ガイドに**Edit**を置き、`openMidiEditor()`(js/midi.js)の編集画面を開く(パネルの「編集する」「範囲を選ぶ」も同じ画面)。
  「ノートを直す」(空いた所をクリックで追加・ドラッグで移動・右端で長さ・ダブルクリック/Deleteキー/ボタンで削除・Ctrl+Z)と
  「書き出す範囲を囲む」の2つの道具。**保存はそのカードに上書き**し`midi.edited`を立てる。コード+旋律は設計図の旋律も
  直した旋律に合わせる(ハネを外して戻す)。音の四角は押すたびに描き直すため`dblclick`イベントが届かず、ダブルクリックは
  pointerdownの間隔で見分けている。編集画面を開いている間はキー入力を止める(カードの編集ガイドのDelete=カード削除に届かないように)。
  方向性は「**編集したものに、さらにいろんなアステリズムをつないでブラッシュアップする**」(ユーザー判断): 「作り直す」は、
  MIDIカードにASTRでつないだカード(MIDI・発言以外)と持ち主のソウルの知識(`window.LyraMidiLinks()`、js/screens/ensemble.js)も
  渡し、つないだものがあればコメントは空でよい。改善版はそのソウルを`memberIds`に引き継ぎ、`linkedNames`をカードに出す。
  `midi.edited`なら手で直した実際の音(パートごと)も渡して尊重させる。旋律を作る経路ではアーティスト名・曲名の項目を外す
- **試聴の音色(Webの音色、2026-09-25、ユーザー要望「編集画面からウェブ音色を選べるように。デフォルトはピアノ」)**:
  FluidR3 GM(gleitz/midi-js-soundfonts)の1音ずつのmp3を、jsDelivr(`cdn.jsdelivr.net/gh/.../FluidR3_GM/<音色>-mp3/<Db4など>.mp3`、
  CORS可)から**鳴らす音の高さの分だけ**読み込む(`prepareVoice()`、ページを開いている間だけ覚える)。音色はカードごとの
  `card.voice`(無ければ**フルート**。当初はピアノだったが、ユーザー要望で変更)で、編集画面の「音色」で選ぶと即カードに残る(ノートの編集と違い「やめる」でも戻さない)。
  カードの試聴・小窓・編集画面の試聴・WAV書き出しがこの音色で鳴る(`scheduleVoiced()`)。ドラム(GM配置)は常に簡易の打楽器音、
  「簡易シンセ」は従来の三角波・のこぎり波。読み込めない時は簡易シンセに落とす。.midの中身には関係しない。
  ファイル名はフラット表記(C#4.mp3は404、Db4.mp3が正しい)。一部のGM名(例: nylon_string_guitar)は無く、acoustic_guitar_nylon
- **CubaseへはフォルダへのMIDI保存を経由する(2026-09-25、実機で判明)**: 最初はChrome/Edgeの`DownloadURL`ドラッグ
  (dragstartで「MIME:ファイル名:blob URL」)だけにしたが、**Cubaseのインストゥルメントトラックへ落とすと禁止マークで
  入らなかった**。Windowsでは実体のファイルでなく「落とされてから中身を渡す仮のファイル」として渡るため、パスを求める
  Cubaseは受け取れないとみている。そこでチップの**クリックで、一度選んだ書き出し先フォルダへ .mid を直接保存**する
  (File System Access API。ハンドルはIndexedDB`lyra-local`に保存、同名があれば「(2)」を付けて上書きしない)。
  そのフォルダをCubaseのMediaBayかエクスプローラーで開いて、そこからトラックへドラッグする。ドラッグ(DownloadURL)は
  デスクトップ・エクスプローラー向けに残してある。小窓の上部に書き出し先と結果を出す。**小窓からのクリックで本体側の
  フォルダ選択ダイアログが開くか(PiPのユーザー操作が本体へ伝わるか)は実機未確認**(テストはフォルダ選択をモック)
- **アンサンブルでは、招集されたソウルの全パラメータを「手持ちの知識」として渡す**(モジュールごとにまとめ、
  つないだカードのものは詳しく、他は効果80字・意図40字)。以前は先頭40件に切っていたため、Serum2(229件)では
  後ろのモジュールが見えなかった(2026-09-25修正)。共通語彙の変換表を作る時も全件を見る
- アンサンブルの招集は「線でつながったカードのまとまり」単位。ホームの舞台は毎回「場(STAGE)」として参加し、
  汎用ペルソナ「楽典」も毎回参加する
- **「アンサンブルを聴く」「コード+旋律で鳴らす」は課題カードの編集ガイドのヘックス**(2026-09-25)。カードの上の外側に
  「聴く」「鳴らす」を並べ、その課題カードを含むまとまりが対象になる(Edit=左、Delete=右、ASTR=下、聴く・鳴らす=上)。
  以前は右下の「招集中」パネルのボタンで、最後にタップ/移動したカードのまとまりが対象だったが、どのカードを中心に
  行われるか分かりにくいというユーザー判断でパネルごと廃止した。最後にタップ/移動したカードのまとまりには今も枠の
  印が付く(`renderMembers()`)
- **アンサンブルのカードは傾けない**(2026-09-25)。モックアップどおり±2度傾けていたが、ユーザー判断で真っ直ぐに。
  古いデータの`card.tilt`は残っているが使わない
- **感想とまとめ(2026-09-25、ユーザー要望「気付きからGeminiAIに感想を聞けるように。ゆくゆくは作曲家AIも参加できるように。
  ConstellationのCrews機能に近い」「サマリー機能もだね」)**: js/screens/ensemble.js。
  - **感想**: 気づきカードの上の「感想」ヘックス → 話し手を選ぶ(`showChoiceDialog`)→ Geminiを1回 → 感想カード(`type: 'comment'`、
    120字程度)を右隣に置き、**線を自動で結ぶ**(MIDIの改善版に続く2つ目の例外。何への感想かを辿れるように。`connection.auto`)。
    渡すのは気づきの文と、線で直接つながったカードの要約だけ(ソウルの知識は渡さない=トークン節約)
  - **まとめ**: 下の道具バーの「まとめ」→ 範囲(最後にタップしたまとまり/この舞台全体)・話し手・問い(任意)→ Geminiを1回 →
    まとめカード(`type: 'summary'`、見出し+200〜400字)。線は結ばない。発言カードは声をつなげて渡し、効いた/効かなかったも含める。
    CONSTELLATIONのサマリーと同じく、**問いがある時は全体の総括を前置きに書かせない**(前置きが8割になった実例があるため、
    「最初の一文から答えを書く」まで指示する)
  - **話し手は`voices()`**: 今は「Gemini」(素。文体の指定なし)と「楽典」(アンサンブルと同じ汎用の音楽理論の専門家)。
    **作曲家AIを参加させる時は`voices()`に足す**(例: 作曲家のソウルから、そのソウルの知識と語り口で話す話し手を作る)。
    話し手が1人なら選ぶダイアログは出さない。CONSTELLATIONと同じく、話し手を登録・保存する時にGeminiは呼ばない方針にする
  - 感想・まとめのカードは編集なし(ASTRとDeleteのみ)。ASTRでつなぐと、アンサンブル・鳴らす・作り直しに文として渡る
- **矩形選択と一括移動(2026-09-25、ユーザー要望「ConstellationのFlight Engineerの機能を簡単に使えるように。Shift+ドラッグで
  矩形選択、Shift+矩形範囲タップ&ドラッグで内のカードを一括移動」)**: `js/marquee.js`。モードは持たずShiftだけで使う。
  Shift+背景ドラッグで囲んだカード(一部でも重なれば)を選び、点線の枠(`contentEl`の中なのでズームに追従)で囲む。
  Shift+枠の中(カードの上でもよい)からのドラッグでまとめて動かし、指を離した時に`onCardMoved`・位置の保存・線の引き直し。
  **viewportの捕獲フェーズのpointerdownで受けてstopPropagationする**ので、カードの長押し(編集ガイド)とinteract.jsのパンより先に
  取れる(パンは`lockPan()`でも止める)。Esc・Shiftなしで背景を押す・画面の切り替えで解除。Shiftの無いスマホでは使えない。
  自動テストのブラウザ操作ツールの合成ドラッグ/クリックはページに届かないことがあったので、確認は合成PointerEventで行った
- **ダイアログは画面の高さに収めて中をスクロール**(2026-09-25、MIDIを作るダイアログが縦に長く「作る」が見切れた指摘)。
  `.modal`に`max-height`+`overflow-y: auto`、`.modal-actions`は`position: sticky`で下に貼り付けて常に見えるようにした
- **画像カード(2026-09-26、ユーザー要望「画像カードを新設し、画像の印象からMIDIを生成できるように」)**: アンサンブルの
  道具バーの「画像」・ドロップ・Ctrl+Vで置く(`type: 'image'`、js/screens/ensemble.js)。**観賞用ではなく「試しては入れ替え」を
  繰り返すための材料**(ユーザー判断。Pixabayの軽い画像をドラッグ&ドロップで入れる想定)なので:
  - **画像はDriveに上げない**。この端末のIndexedDB(`lyra-images`、キーはカードID。js/app.js の`putLocalImage()`など)に
    長辺512px・JPEG品質0.72で置く。Driveに上げると、Driveのファイルを消さない決まりのため入れ替えるたびに増え続ける。
    Driveのデータに残るのは名前・印象の文・試した履歴だけ。別の端末では「この端末に画像がありません」と出て、ドロップで入れ直せる。
    カードを削除するとIndexedDBの画像も消す(端末内の一時置き場なのでDriveの削除禁止の対象外)
  - **入れ替え**: 画像カードへのドロップ/左下の「入替」ヘックス/パネルのボタン/そのカードを選んだままCtrl+V。位置・線はそのまま、
    印象は空に戻し、前の画像の名前と印象を`card.tried`(最大20件)に残す
  - 上の「鳴らす」(コード+旋律)・「ビート」で、画像をそのまま`files`でGeminiに添付する。課題カードの「鳴らす」「ビート」も、
    まとまりに画像カードがあれば添付する(画像があればソウルが無くても「鳴らす」を使える)。1回に最大3枚。
    印象の文(Editで書ける)が空の画像は、**同じ1回の出力の`impressions`**に視覚の言葉のまま印象を書かせてカードに残す
    (呼び出し回数は増やさない)。実機のGeminiで、画像の印象がMIDIに出ること・印象がカードに書き込まれることをユーザーが確認済み(2026-09-26)
  - **画像検索(Pixabay API、`js/imagesearch.js`、2026-09-26、ユーザー判断)**: 道具バーの「画像」と、画像カードの「入替」で
    キャンバスを隠さない浮いたパネルが開く。**開いたままサムネイルを次々クリックすると、同じカードの画像がその場で入れ替わる**
    (最初のクリックでカードが無ければ置き、以後はそのカード)。「PCのファイルから」も同じパネルにある。キーは設定画面の任意欄で
    `lyra.pixabayApiKey`(localStorage)。取り込むのは`webformatURL`(640px)で、端末内に512pxで置く。カードの表示に
    PixabayのURLは使わない(規約: ホットリンクしない)。`card.source`に出どころ(pageURL・user)を残し、パネルからPixabayへ飛べる。
    規約どおり、検索結果に「画像: Pixabay」を出し、同じ検索は24時間キャッシュする(localStorageに直近10件。同じオリジンの
    CONSTELLATIONと容量を共有するため小さく)。APIも画像のCDNも`Access-Control-Allow-Origin: *`(curlで確認)。
    本物のAPIキーでの検索・取り込みは公開URLでユーザーが動作を確認済み(2026-09-26)。取得手順はREADME.md手順9
- **MIDI生成モデル(2026-09-26、ユーザー構想)**: MIDIの生成方法を「モデル」として並べ、選べるようにしていく。
  **今のMIDI生成(コード+旋律・ビート・発言からのMIDI・作り直し)を「基本の楽典モデル」として`models/gakuten.md`に書き起こした**
  (実装が実際にしていることの記録。コードを変えたらこのMDも直す)。他のAIチャットで作ったメソッドを別の拡張モデルのMDとして
  受け取り、LYRA上で動くようにする予定(モデルを選ぶ仕組みはまだ無い)
- **アンサンブル画面でShift+A**: 全ソウル(今の舞台を除く)の一覧を区分ごとに出し、クリックしたソウルのカードを置く
  (対象のカードがあればその右隣)。線は自動で結ばない(Asterismは手動接続のみ)。入力欄にフォーカスがある時と、
  ダイアログを表示中は反応しない
- オーディオは既定でOpus/Ogg(約96kbps)に圧縮して保存(WebCodecsのAudioEncoder+自前のOggの
  カプセル化)。AudioEncoderが無いブラウザではモノラル22.05kHzのWAV。「元の音質のまま」は取り込み時の選択のみ

## 現在の状態

- 段階1〜6(入口画面・ソウル画面・解体パイプライン・アンサンブル・MIDI/WAV/レシピ・日次課題)を実装済み。
  Drive・サインイン・GeminiをモックにしたローカルのテストページでPCのChromeで動作確認した。
  **実機(本物のGemini・Drive・スマホ)ではまだ確認していない**
- GCPプロジェクト`LYRA`・OAuthクライアント・Gemini APIキーの発行、GitHub Pages公開まで完了(2026-09-25)。
  OAuth同意画面は**本番環境**(「テスト」ではテストユーザーが保存されず403になった。README.md手順4参照)
- 実機で確かめたいこと: PDFの解体(Files API)、スマホでの右パネル(下からのシート)とモジュールタブ、
  長押しでのパラメータのピン留め、Oggの再生(iPhoneのSafari)、MIDI書き出しをCubaseで読めるか
